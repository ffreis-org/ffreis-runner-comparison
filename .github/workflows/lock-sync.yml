name: Lock Sync

on:
  pull_request:
    branches: [ main ]
    paths:
      - "pyproject.toml"
      - "uv.lock"
      - ".github/workflows/lock-sync.yml"
  push:
    branches: [ main ]
    paths:
      - "pyproject.toml"
      - "uv.lock"
      - ".github/workflows/lock-sync.yml"

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lock-sync:
    uses: FelipeFuhr/ffreis-workflows-python/.github/workflows/python-lock-sync.yml@1ce012b023b91e4c293846af4cfd93062d36d1be
    with:
      python-version: "3.11"
