name: DevOps / ShellCheck

on:
  pull_request:
  push:
    branches: [main, master]

jobs:
  shellcheck:
    uses: FelipeFuhr/ffreis-workflows-general/.github/workflows/general-shellcheck.yml@v1.2.0
    with:
      working-directory: .
      fail-on-error: true
