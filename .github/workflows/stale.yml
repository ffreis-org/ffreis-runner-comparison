name: DevOps / Stale

on:
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  stale:
    uses: FelipeFuhr/ffreis-workflows-general/.github/workflows/general-stale.yml@v1.2.0
    with:
      only: "both"
